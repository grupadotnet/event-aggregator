node {
    deleteDir()

    try {
        stage ('Clone') {
        	checkout scm
        }
        stage ('Build') {
            bat "dotnet build"
        }
    } catch (err) {
        currentBuild.result = 'FAILED'
        throw err
    }
}