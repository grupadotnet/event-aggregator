node {
    deleteDir()

    try {
        stage ('Clone') {
        	checkout scm
        }
        stage ('Build') {
            bat "dotnet build"
        }
        stage ('Test') {
            bat "dotnet test"
        }
    } catch (err) {
        currentBuild.result = 'FAILED'
        throw err
    }
}